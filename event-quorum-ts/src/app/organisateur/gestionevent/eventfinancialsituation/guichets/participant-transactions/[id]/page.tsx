import { ParticipantTransactionsView } from '../participant-transactions-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Détails des transactions du participant',
};

export default function Page() {
  return <ParticipantTransactionsView />;
}